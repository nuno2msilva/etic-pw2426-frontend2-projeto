{
  "buildCommand": "cd server && npx prisma generate && cd .. && npx prisma generate && next build",
  "installCommand": "npm install && cd server && npm install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
