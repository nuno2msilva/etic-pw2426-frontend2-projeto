{
  "buildCommand": "cd server && npx prisma generate && cd .. && npx prisma generate && next build --webpack",
  "installCommand": "npm install && cd server && npm install",
  "framework": "nextjs"
}
