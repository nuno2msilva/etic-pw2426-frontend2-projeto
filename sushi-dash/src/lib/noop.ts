// Empty module — replaces Next.js's built-in polyfill bundle.
// Safe because our browserslist already natively supports every polyfill
// that Next.js ships: Array.at, Array.flat, Object.hasOwn, Object.fromEntries,
// String.trimStart/trimEnd (all baseline in Chrome 105+, Safari 15.4+, Firefox 104+)
export {};
